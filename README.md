# Makitso Development Plugin

## Provides Commands

Command              | Description
---------------------|--------------------
`set <prop> <value>` | Set a context value
`get <prop>`         | Get a context value
`dumpStore <store>`  | Dump the store
`dumpSchema`         | Dump the schema
`debugOn`            | Turn on debugging
`debugOff`           | Turn off debugging
